{
  "name": "gatsby-plugin-reactfire",
  "version": "0.1.2",
  "description": "`FirebaseAppProvider` component from `reactfire` as a Gatsby plugin.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo \"No build script setup\""
  },
  "keywords": [
    "gatsby",
    "gatsby-plugin",
    "firebase",
    "reactfire"
  ],
  "author": "@piducancore",
  "license": "0BSD",
  "peerDependencies": {
    "firebase": "^9.6.5",
    "reactfire": "^4.2.1"
  }
}
